global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  # - "first_rules.yml"
  # - "second_rules.yml"

scrape_configs:
  # BatchFlow 集成测试指标
  - job_name: 'batchflow-integration-test'
    static_configs:
      - targets: ['batchflow-mysql-test:8080', 'batchflow-postgres-test:8080', 'batchflow-sqlite-test:8080', 'batchflow-redis-test:8080']
    scrape_interval: 10s
    metrics_path: /metrics
    scrape_timeout: 5s

  # Prometheus 自身指标
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']